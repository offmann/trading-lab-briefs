GitHub Pages site (source: `/docs` on `main`).

Hub pages: `index.html` (Investor / home), `leaderboard.html` (ranked sleeves + $1,000 plot), `experiments.html` (brief gallery). Old `investor.html` / `progress.html` / `keepers.html` redirect. Experiment cards stay in `briefs/`. See `UX.md` for IA, privacy rules, and the CI rebuild.
