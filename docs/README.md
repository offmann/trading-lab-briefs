GitHub Pages site (source: `/docs` on `main`).

Hub pages: `index.html` (overview), `progress.html` (timeline), `keepers.html` (survivors). Experiment cards stay in `briefs/`. See `UX.md` for IA and privacy rules.
