GitHub Pages site (source: `/docs` on `main`).

Hub pages: `index.html` (Investor / home), `leaderboard.html` (ranked sleeves + $1,000 plot), `experiments.html` (brief gallery). `investor.html` is a full Investor duplicate. `progress.html` / `keepers.html` are Experiments-equivalent aliases (never blank). Experiment cards stay in `briefs/` with a Back to Investor · Leaderboard · Experiments bar. See `UX.md` for IA, privacy rules, and the CI rebuild.
