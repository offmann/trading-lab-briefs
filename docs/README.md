GitHub Pages site (source: `/docs` on `main`).

Hub pages: `index.html` (overview), `progress.html` (timeline), `keepers.html` (survivors), `investor.html` (plain-language digest), `leaderboard.html` (ranked sleeves). Experiment cards stay in `briefs/`. See `UX.md` for IA, privacy rules, and the CI rebuild.
